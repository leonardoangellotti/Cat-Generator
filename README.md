# Cat Generator

A simple project for generating cat images using machine learning and generative models.

## Features

- Cat image generation
- Training and inference notebooks
- Model experimentation
- Result visualization

## Structure

- `model/` — the trained model
- `sample/` — saved generated sample
- `slide/` — presentation slides

## Usage

Run the notebooks locally 
